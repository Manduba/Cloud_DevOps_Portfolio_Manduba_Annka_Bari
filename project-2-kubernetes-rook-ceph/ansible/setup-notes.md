# Setup Notes

The original setup playbook was used for SSH access configuration during the academic project.

The full setup file is not included in this public portfolio because it involved access/key configuration.

No private keys, grader keys, or environment-specific SSH details are included in this repository.